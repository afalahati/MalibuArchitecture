import React from 'react';
import ReactDOM from 'react-dom/client';
import MalibuArchitectureApp from './MalibuArchitectureApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MalibuArchitectureApp />
  </React.StrictMode>
);
